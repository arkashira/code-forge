# Code Forge
A Python project for CI integration and validation.

## Usage
1. Run the unit tests using `python -m pytest`.
2. Load a lockfile using `load_lockfile` function.
3. Run unit tests using `run_unit_tests` function.
4. Validate the lockfile using `validate_lockfile` function.
