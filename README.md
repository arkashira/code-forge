# Library API
A simple API for managing libraries.

## Usage
1. Create a `Library` object with the desired name, version, and code.
2. Add the library to the `LibraryAPI` instance using the `add_library` method.
3. Use the `get_library` method to retrieve a library by name and version.
4. Use the `handle_request` method to handle incoming requests.
